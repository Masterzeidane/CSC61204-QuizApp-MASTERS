/**
 * Builder pattern — Step-by-step construction of complex objects.
 *
 * <p>Suggested classes:</p>
 * <ul>
 *   <li>{@code QuizBuilder}     — fluent builder for constructing Quiz objects</li>
 * </ul>
 *
 * <p>Note: if the Builder is simple enough, consider making it an inner
 * class of {@code com.groupname.quizapp.model.Quiz} instead.</p>
 */
package com.groupname.quizapp.builder;
