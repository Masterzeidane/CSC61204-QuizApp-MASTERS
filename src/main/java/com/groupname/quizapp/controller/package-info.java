/**
 * Controller layer (MVC) — Coordinates models and views, handles user actions.
 *
 * <p>Suggested classes:</p>
 * <ul>
 *   <li>{@code QuizController}  — orchestrates quiz flow (load → present → score)</li>
 * </ul>
 */
package com.groupname.quizapp.controller;
